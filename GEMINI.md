# Trust Safety Intake

If the user asks for a BRD, PRD, business case, or case study, the first command is `trust-intake init`. Never write that document in chat or as a markdown file.

Read `skills/trust-intake/SKILL.md`. Interview only. Run the CLI.

Do not run `trust-intake approve`. Print the `--confirm` command from `memo`. The author runs it.

You are not done until `trust-intake decide --run <id>` or the author has run `trust-intake decide --triage <folder>`.
